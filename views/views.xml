<odoo>
  <data>

    <record id="view_sincronizar_productos_wizard" model="ir.ui.view">
        <field name="name">Sincronizar Productos</field>
        <field name="model">sinc_productos.sinc_productos</field>
        <field name="arch" type="xml">
            <form string="Parameters">
                <group>
                    <label string="Sincronizar Productos."/>
                </group>
                <footer>
                    <button name="sincronizar_productos" string="Sincronizar" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-default" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record id="action_sincronizar_productos" model="ir.actions.act_window">
        <field name="name">Sincronizar Productos</field>
        <field name="res_model">sinc_productos.sinc_productos</field>
        <field name="view_type">form</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <menuitem action="action_sincronizar_productos" id="menu_sincronizar_productos" parent="stock.menu_stock_config_settings" sequence="20" groups="base.group_no_one"/>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="sinc_productos.list">
      <field name="name">sinc_productos list</field>
      <field name="model">sinc_productos.sinc_productos</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="sinc_productos.action_window">
      <field name="name">sinc_productos window</field>
      <field name="res_model">sinc_productos.sinc_productos</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="sinc_productos.action_server">
      <field name="name">sinc_productos server</field>
      <field name="model_id" ref="model_sinc_productos_sinc_productos"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="sinc_productos" id="sinc_productos.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="sinc_productos.menu_1" parent="sinc_productos.menu_root"/>
    <menuitem name="Menu 2" id="sinc_productos.menu_2" parent="sinc_productos.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="sinc_productos.menu_1_list" parent="sinc_productos.menu_1"
              action="sinc_productos.action_window"/>
    <menuitem name="Server to list" id="sinc_productos" parent="sinc_productos.menu_2"
              action="sinc_productos.action_server"/>
    -->
  </data>
</odoo>
